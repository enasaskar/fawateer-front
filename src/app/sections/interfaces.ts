export interface IBenefit {
  icon: string;
  description: string;
}

export interface IFeature {
  icon: string;
  label: string;
  description: string;
}

export interface IAboutItem {
  icon: string;
  label: string;
  description: string;
}